# Выполнение Лабораторной работы №10

План включает создание двух независимых веб-приложений с нуля в папке `lab-10/Solution`: React To-Do приложения с использованием Vite и сайта-портфолио на Next.js. Для обоих проектов будет изменена структура компонентов и переменные, а также добавлены подробные комментарии.

## User Review Required

- Требуется подтверждение для запуска команд установки и создания проектов через `npm create vite` и `npx create-next-app` в папке `Solution`. Я создам эту папку и запущу в ней инициализацию.

## Proposed Changes

### Настройка рабочей среды

- Создание директории `d:\1mukhamedzhanov\mukhamedjanov\lab-10\Solution`.

### Часть 1: React To-Do List (Vite)

- Инициализация проекта `todo-app` с шаблоном `react-ts`.
- Установка Tailwind CSS и его настройка.
- Реализация логики (переименование переменных, вынос компонентов). Вместо того чтобы писать всё в одном `App.tsx`, я разделю код на небольшие компоненты: `TaskInput.tsx`, `TaskList.tsx`, `TaskItem.tsx`, `TaskStats.tsx`, чтобы выполнить требование по изменению структуры.
- Реализация удаления, изменения статуса, добавление статистики и состояния пустого списка.
- Комментарии к ключевым методам (`useState`, `map`, `filter` и др.).

### Часть 2: Next.js Portfolio Site

- Инициализация проекта `portfolio-site` с использованием `npx create-next-app@latest`.
- Установка пакета `date-fns`.
- Разработка страниц с измененной структурой и стилизацией:
  - `app/page.tsx` (Главная)
  - `app/about/page.tsx` (Обо мне)
  - `app/blog/page.tsx`, `app/blog/data.ts`, `app/blog/[slug]/page.tsx`, `app/blog/[slug]/not-found.tsx` (Блог с динамической маршрутизацией)
  - `app/projects/page.tsx`, `app/components/ProjectCard.tsx` (Проекты)
- Добавление подробных комментариев к методам генерации статических путей (`generateStaticParams`) и серверным компонентам.

## Verification Plan

### Automated Tests
- Запуск сборки `npm run build` для обоих проектов, чтобы убедиться в отсутствии ошибок TypeScript и проблем компиляции.

### Manual Verification
- После генерации кода я проверю, корректно ли созданы файлы и соответствуют ли они требованиям лабораторной работы.
